"use client";

/** Contact page wrapper — section backgrounds match homepage; gradients live on cards only. */
export function ContactPageTheme({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
